import React from 'react';
import { Button } from 'react-bootstrap';

function Homee() {
  return (
  
  <div>
  <Button> contact list</Button>
  <Button> contact Add</Button>
  </div>
  
  );
}

export default Homee;
